import { NextConfig } from 'next';

const config: NextConfig = {
  images: {
    domains: [
      'lh3.googleusercontent.com',
      'images.unsplash.com',
    ],
  },
  /* config options here */
};

export default config;
